import { View, Text } from 'react-native';
import React from 'react';

const CartScreen = () => {
    return (
        <View className="flex justify-center items-center h-full">
            <Text className="text-xl">CartScreen</Text>
        </View>
    );
};

export default CartScreen;
